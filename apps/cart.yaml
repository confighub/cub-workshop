apiVersion: v1
kind: Namespace
metadata:
  name: retail
  labels: {team: retail-platform}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart
  namespace: retail
  labels: {app: cart, team: retail-platform}
spec:
  replicas: 2
  selector:
    matchLabels: {app: cart}
  template:
    metadata:
      labels: {app: cart, team: retail-platform}
    spec:
      containers:
        - name: cart
          image: registry.k8s.io/pause:3.10
          resources:
            requests: {cpu: 50m, memory: 64Mi}
            limits: {cpu: 200m, memory: 128Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: cart
  namespace: retail
  labels: {app: cart, team: retail-platform}
spec:
  selector: {app: cart}
  ports:
    - port: 80
      targetPort: 8080
