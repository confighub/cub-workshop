apiVersion: v1
kind: Namespace
metadata:
  name: retail
  labels: {team: retail-platform}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout
  namespace: retail
  labels: {app: checkout, team: retail-platform}
spec:
  replicas: 2
  selector:
    matchLabels: {app: checkout}
  template:
    metadata:
      labels: {app: checkout, team: retail-platform}
    spec:
      containers:
        - name: checkout
          image: registry.k8s.io/pause:3.10
          resources:
            requests: {cpu: 50m, memory: 64Mi}
            limits: {cpu: 200m, memory: 128Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: checkout
  namespace: retail
  labels: {app: checkout, team: retail-platform}
spec:
  selector: {app: checkout}
  ports:
    - port: 80
      targetPort: 8080
