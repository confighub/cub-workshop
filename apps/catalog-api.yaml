apiVersion: v1
kind: Namespace
metadata:
  name: retail
  labels: {team: retail-platform}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-api
  namespace: retail
  labels: {app: catalog-api, team: retail-platform}
spec:
  replicas: 2
  selector:
    matchLabels: {app: catalog-api}
  template:
    metadata:
      labels: {app: catalog-api, team: retail-platform}
    spec:
      containers:
        - name: catalog-api
          image: registry.k8s.io/pause:3.10
          resources:
            requests: {cpu: 50m, memory: 64Mi}
            limits: {cpu: 200m, memory: 128Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-api
  namespace: retail
  labels: {app: catalog-api, team: retail-platform}
spec:
  selector: {app: catalog-api}
  ports:
    - port: 80
      targetPort: 8080
