name: stack
version: 0.2.0
commands:
    - name: stack
      summary: 'A certified composition of components, spoken by name: certify, sandbox, and upload stacks built from digest-pinned certified bundles'
      entrypoint: bin/cub-stack
    - name: fleet
      summary: 'Placement as data: build, age, and read a whole governed fleet from a fleet manifest'
      entrypoint: bin/cub-fleet
